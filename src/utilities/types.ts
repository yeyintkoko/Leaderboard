export interface LeaderType {
    id: string;
    name: string;
    bananas: number;
}

export interface LeaderCardType {
    id: string;
    name: string;
    bananas: number;
    rank: number;
    searched: boolean;
}
